# Command to start on workspace startup (optional)
tasks:
  - init: cd 2021/slides/session/ && git clone https://github.com/hakimel/reveal.js.git && cp index.html reveal.js/ && cd reveal.js && npm install
    command: npm start
# Ports to expose on workspace startup (optional)
ports:
  - port: 8000
    onOpen: open-preview
